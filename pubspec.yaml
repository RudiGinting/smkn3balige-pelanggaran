name: smkn3balige
description: Aplikasi Mobile SMK Negeri 3 Balige
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  http_parser: ^4.0.2
  mime: ^1.0.5
  flutter:
    sdk: flutter
  provider: ^6.1.2
  http: ^1.2.1
  image_picker: ^1.1.2
  cached_network_image: ^3.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
